---
phase: 06-ai-integration-tooling
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - .claude/skills/build-presentation/SKILL.md
  - .claude/skills/build-presentation/workflows/build-new-deck.md
  - .claude/skills/build-presentation/workflows/refine-deck.md
  - .claude/skills/build-presentation/workflows/extract-theme.md
  - CLAUDE.md
autonomous: true
requirements: [AI-01]

must_haves:
  truths:
    - "SKILL.md classifies user intent and routes to the correct workflow in under 150 lines"
    - "build-new-deck workflow orchestrates the full pipeline: discuss -> brief.md -> research -> deck-plan.md -> review -> presentation.html"
    - "refine-deck workflow handles iteration on existing presentations"
    - "extract-theme workflow wraps the existing tools/extract-theme.js"
    - "CLAUDE.md teaches Claude Code the framework with component summary, theme overview, and skill invocation instructions"
  artifacts:
    - path: ".claude/skills/build-presentation/SKILL.md"
      provides: "Skill router for presentation builder"
      contains: "build-new-deck"
    - path: ".claude/skills/build-presentation/workflows/build-new-deck.md"
      provides: "Full deck creation pipeline"
      contains: "deck-plan.md"
    - path: ".claude/skills/build-presentation/workflows/refine-deck.md"
      provides: "Iteration workflow for existing decks"
      contains: "presentation.html"
    - path: ".claude/skills/build-presentation/workflows/extract-theme.md"
      provides: "PPTX theme extraction workflow"
      contains: "extract-theme"
    - path: "CLAUDE.md"
      provides: "Framework teaching content for Claude Code"
      contains: "Presentation Builder Framework"
  key_links:
    - from: "SKILL.md"
      to: "workflows/*.md"
      via: "routing table maps user intent to workflow file paths"
      pattern: "workflows/"
    - from: "workflows/build-new-deck.md"
      to: ".claude/agents/presentation-*.md"
      via: "workflow spawns subagents at each pipeline step"
      pattern: "presentation-researcher|presentation-strategist|presentation-builder"
    - from: "CLAUDE.md"
      to: ".claude/skills/build-presentation/SKILL.md"
      via: "framework section tells Claude how to invoke the skill"
      pattern: "build-presentation"
---

<objective>
Create the skill router (SKILL.md), three workflows, and update CLAUDE.md with framework teaching content. This is the user-facing entry point for AI-assisted presentation building.

Purpose: The skill layer turns the raw framework (templates, tokens, themes) into a guided AI experience. SKILL.md routes user requests, workflows orchestrate multi-step pipelines, and CLAUDE.md teaches Claude Code the full system.

Output: SKILL.md, 3 workflow files, updated CLAUDE.md
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai-integration-tooling/06-CONTEXT.md
@.planning/phases/06-ai-integration-tooling/06-RESEARCH.md
@.planning/phases/06-ai-integration-tooling/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SKILL.md router and three workflow files</name>
  <files>
    .claude/skills/build-presentation/SKILL.md
    .claude/skills/build-presentation/workflows/build-new-deck.md
    .claude/skills/build-presentation/workflows/refine-deck.md
    .claude/skills/build-presentation/workflows/extract-theme.md
  </files>
  <read_first>
    ~/.claude/skills/expertise/build-nextjs-apps/SKILL.md
    ~/.claude/skills/expertise/build-nextjs-apps/workflows/build-new-app.md
    ~/.claude/agents/gsd-phase-researcher.md
    ~/.claude/agents/gsd-executor.md
    .claude/skills/build-presentation/references/component-catalog.md
    .claude/skills/build-presentation/references/audience-presets.md
    .planning/phases/06-ai-integration-tooling/06-RESEARCH.md
  </read_first>
  <action>
    Create directories `.claude/skills/build-presentation/workflows/` if they do not exist.

    **SKILL.md** (per D-01, D-02 -- router only, 100-150 lines):
    Follow the build-nextjs-apps SKILL.md pattern. Structure:
    - YAML frontmatter: `name: build-presentation`, `description: Build consulting-grade presentations...`
    - `<essential_principles>`: Brief framework overview -- 14 component templates in templates/, CSS tokens in tokens/, themes in themes/, projects in projects/{name}/. Presentations are single HTML files using reveal.js. German-first with consulting quality.
    - `<intake>`: Three options: (1) Build a new presentation, (2) Refine an existing deck, (3) Import a corporate theme from .pptx
    - `<routing>`: Table mapping user intent keywords to workflow files:
      - "new", "create", "build", "presentation", "deck" -> workflows/build-new-deck.md
      - "refine", "improve", "update", "change", "fix", "iterate" -> workflows/refine-deck.md
      - "theme", "import", "pptx", "brand", "corporate", "extract" -> workflows/extract-theme.md
    - `<verification_loop>`: Post-generation checks -- HTML file opens in browser, all slides render, theme applied correctly, master layer visible, no broken layout
    - `<reference_index>`: List all 5 reference files with 1-line descriptions. Path format: `references/component-catalog.md`

    **workflows/build-new-deck.md** (per D-03, D-11, D-12):
    Full pipeline workflow. Structure:
    - H1: "Workflow: Build New Deck"
    - `<required_reading>`: references/component-catalog.md, references/audience-presets.md, references/design-principles.md
    - `<process>` with these steps:
      - Step 1: Discuss with User (inline, per D-08). Use AskUserQuestion to gather: topic, audience type, key messages (3-5 points), slide count preference, theme preference, any specific components wanted. Produce `projects/{name}/brief.md`.
      - Step 2: Research (optional, per D-06). If user wants real data/stats, spawn `presentation-researcher` subagent pointing at brief.md. Output: `projects/{name}/research.md`.
      - Step 3: Strategize (per D-07). Spawn `presentation-strategist` subagent with brief.md (and research.md if exists). Output: `projects/{name}/deck-plan.md`.
      - Step 4: User Review. Present deck-plan.md summary. Wait for approval or revision requests. Loop back to Step 3 if changes needed.
      - Step 5: Build. Spawn `presentation-builder` subagent with deck-plan.md. Output: `projects/{name}/presentation.html`.
      - Step 6: Iterate. Offer to refine -- route to refine-deck.md workflow for changes.
    - Project folder convention per D-12: `projects/{name}/` containing brief.md, research.md (optional), deck-plan.md, presentation.html, notes.yaml (optional)

    **workflows/refine-deck.md** (per D-03):
    Iteration workflow. Structure:
    - H1: "Workflow: Refine Deck"
    - `<required_reading>`: references/component-catalog.md, references/design-principles.md
    - `<process>`:
      - Step 1: Identify target. Find existing `projects/{name}/presentation.html` and `deck-plan.md`.
      - Step 2: Understand changes. Ask user what to change (content, layout, theme, specific slides, overall structure).
      - Step 3: Update plan if structural. For component or flow changes, update deck-plan.md first, get approval.
      - Step 4: Apply changes. For content-only changes, edit presentation.html directly. For structural changes, spawn presentation-builder with updated deck-plan.md.
      - Step 5: Verify. Open in browser, check affected slides.

    **workflows/extract-theme.md** (per D-03):
    PPTX import wrapper. Structure:
    - H1: "Workflow: Extract Theme from PPTX"
    - `<required_reading>`: references/theme-system.md
    - `<process>`:
      - Step 1: Get .pptx file path from user.
      - Step 2: Run `node tools/extract-theme.js <input.pptx> <theme-name>`. Document expected output: themes/{name}/theme.css + extracted logo images.
      - Step 3: Review extracted theme. Read generated theme.css, check token values.
      - Step 4: Adjust. Fine-tune any tokens that need manual override (common: font fallbacks, shadow values, footer text).
      - Step 5: Test. Apply theme to a quick test presentation to verify rendering.
  </action>
  <verify>
    <automated>test -f .claude/skills/build-presentation/SKILL.md && test -f .claude/skills/build-presentation/workflows/build-new-deck.md && test -f .claude/skills/build-presentation/workflows/refine-deck.md && test -f .claude/skills/build-presentation/workflows/extract-theme.md && wc -l .claude/skills/build-presentation/SKILL.md && echo "ALL 4 FILES EXIST"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md exists and is under 150 lines (`wc -l` returns less than 150)
    - SKILL.md contains YAML frontmatter with "name: build-presentation"
    - SKILL.md contains routing table with strings "build-new-deck", "refine-deck", "extract-theme"
    - SKILL.md contains "reference_index" section listing all 5 reference files
    - build-new-deck.md contains the strings "brief.md", "deck-plan.md", "presentation.html", "presentation-researcher", "presentation-strategist", "presentation-builder"
    - build-new-deck.md references project folder convention "projects/"
    - refine-deck.md contains the string "presentation.html"
    - extract-theme.md contains the string "tools/extract-theme.js"
  </acceptance_criteria>
  <done>Skill router classifies 3 intents, each workflow documents a complete pipeline with steps, required reading, and subagent orchestration</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md with framework teaching content</name>
  <files>
    CLAUDE.md
  </files>
  <read_first>
    CLAUDE.md
    .claude/skills/build-presentation/SKILL.md
    .claude/skills/build-presentation/references/component-catalog.md
    .claude/skills/build-presentation/references/theme-system.md
    templates/_skeleton.html
    tokens/base.css
    package.json
  </read_first>
  <action>
    Per D-18, D-19, D-20: Update CLAUDE.md while preserving ALL GSD comment markers.

    The file has these marker pairs that MUST remain intact:
    - `<!-- GSD:project-start -->` ... `<!-- GSD:project-end -->`
    - `<!-- GSD:stack-start -->` ... `<!-- GSD:stack-end -->`
    - `<!-- GSD:conventions-start -->` ... `<!-- GSD:conventions-end -->`
    - `<!-- GSD:architecture-start -->` ... `<!-- GSD:architecture-end -->`
    - `<!-- GSD:workflow-start -->` ... `<!-- GSD:workflow-end -->`
    - `<!-- GSD:profile-start -->` ... `<!-- GSD:profile-end -->`

    **Add framework teaching content ABOVE the first GSD marker** (per D-19). Insert a new section before `<!-- GSD:project-start -->`:

    ```markdown
    ## Presentation Builder Framework

    A code-based slide deck framework for consulting-grade presentations. AI assistants generate single-file HTML presentations using reveal.js, CSS design tokens, and 14 component templates.

    ### Quick Start
    To build a presentation, invoke the skill: describe what you want to build, the audience, and key messages. The skill routes to the appropriate workflow.

    Skill location: `.claude/skills/build-presentation/SKILL.md`

    ### Component Library (14 layouts)
    [One-line summary table of all 14 components: name | use-when. Keep it to ~20 lines total. Point to references/component-catalog.md for full details.]

    ### Theme System
    Themes override CSS custom properties (tokens) in `tokens/base.css`. Three bundled themes: default, startup, enterprise. Import from PPTX via `tools/extract-theme.js`. See references/theme-system.md.

    ### Audience Presets
    Six audience types with quantified design rules: C-Suite, Stakeholder, Technical, Sales, Workshop, Internal. See references/audience-presets.md.

    ### Project Structure
    ```
    projects/{name}/
      brief.md            # User requirements
      research.md          # Optional research data
      deck-plan.md         # Slide-by-slide plan (review before building)
      presentation.html    # Final output
      notes.yaml           # Optional speaker notes
    ```

    ### Key Conventions
    - German-first: all templates handle 130-300% text expansion
    - BEM-lite CSS: `comp-{name}`, `comp-{name}__{element}`
    - Single HTML output: self-contained, works offline
    - No build step: AI writes final HTML directly
    ```

    **Populate the Technology Stack section** (between GSD:stack-start and GSD:stack-end) with:
    - reveal.js 5.2.1 (CDN) -- presentation engine
    - Inter (Google Fonts CDN) -- sole default font
    - CSS Custom Properties -- design token system
    - DeckTape 3.15.0 (optional) -- PDF export
    - adm-zip + fast-xml-parser (optional) -- PPTX theme extraction

    **Populate the Conventions section** (between GSD:conventions-start and GSD:conventions-end) with:
    - BEM-lite naming: `comp-{name}` wrapper, `comp-{name}__{element}` children
    - Comment headers on all templates: COMPONENT, USE WHEN, REQUIRED/OPTIONAL SLOTS, MASTER LAYER, LAYOUT NOTES
    - 8px spacing grid via CSS custom properties (`--space-xs` through `--space-3xl`)
    - German text handling: `overflow-wrap: break-word`, `hyphens: auto` on all containers
    - Project folder convention: `projects/{name}/` with brief.md, deck-plan.md, presentation.html
    - Master layer: logo, footer bar, slide number -- configurable via presentationConfig

    **Populate the Architecture section** (between GSD:architecture-start and GSD:architecture-end) with:
    ```
    templates/          # 14 component HTML templates + _skeleton.html + index.md
    tokens/             # base.css (design tokens) + animations.css
    themes/             # default/, startup/, enterprise/ theme CSS files
    tools/              # extract-theme.js, export-pdf.sh, gallery.html
    projects/           # Generated presentations ({name}/ folders)
    docs/               # german-typography.md, speaker-notes.md
    .claude/skills/     # build-presentation skill (SKILL.md + workflows/ + references/)
    .claude/agents/     # presentation-researcher, strategist, builder subagents
    ```
  </action>
  <verify>
    <automated>grep -q "Presentation Builder Framework" CLAUDE.md && grep -q "GSD:project-start" CLAUDE.md && grep -q "GSD:workflow-end" CLAUDE.md && grep -q "reveal.js" CLAUDE.md && grep -q "BEM-lite" CLAUDE.md && grep -q "templates/" CLAUDE.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains "## Presentation Builder Framework" ABOVE the line "GSD:project-start"
    - CLAUDE.md contains all 6 GSD marker pairs (project, stack, conventions, architecture, workflow, profile)
    - Technology Stack section (between GSD:stack markers) contains "reveal.js" and "DeckTape"
    - Conventions section (between GSD:conventions markers) contains "BEM-lite" and "8px"
    - Architecture section (between GSD:architecture markers) contains "templates/" and "tokens/" and ".claude/skills/"
    - Framework section references ".claude/skills/build-presentation/SKILL.md"
    - Framework section contains a component summary table with "title" and "framework" component names
  </acceptance_criteria>
  <done>CLAUDE.md teaches Claude Code the full framework while preserving all GSD managed sections</done>
</task>

</tasks>

<verification>
- SKILL.md exists and is under 150 lines with routing to 3 workflows
- All 3 workflow files exist with step-by-step pipelines
- CLAUDE.md has framework teaching content above GSD markers
- All 6 GSD marker pairs preserved in CLAUDE.md
- Technology Stack, Conventions, Architecture sections populated
</verification>

<success_criteria>
A fresh Claude Code session opening this project reads CLAUDE.md and immediately understands the framework, can invoke the skill, and the skill routes to the correct workflow based on user intent.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-integration-tooling/06-03-SUMMARY.md`
</output>
