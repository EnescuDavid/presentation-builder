---
phase: 02-visual-vocabulary
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [VIS-01, VIS-02]

must_haves:
  truths:
    - "The strategist can look up any content type and know which component + visual treatment to use"
    - "The strategist applies a bullet-list smell test before defaulting to text-heavy"
    - "Every content archetype has 3-5 machine-actionable content signals and a fallback component"
    - "Component catalog cross-references archetypes so builder knows the archetype context"
  artifacts:
    - path: ".claude/skills/build-presentation/references/visual-vocabulary.md"
      provides: "15 content archetypes with detection signals, component mapping, Lucide icons"
      contains: "## List of Items"
    - path: ".claude/skills/build-presentation/references/component-catalog.md"
      provides: "Existing catalog extended with archetype cross-references per component"
      contains: "Archetypes"
---

<objective>
Create the visual vocabulary reference that maps 15 content archetypes to visual treatments, including bullet-list smell test and curated Lucide icon set. Extend the component catalog with archetype cross-references.

Purpose: AI agents currently default to text-heavy + bullet lists. This reference gives the strategist a visual decision vocabulary — mapping content types to specific components and treatments. The strategist MUST classify every slide into an archetype; the archetype drives component selection and visual treatment.

Output: One new reference file + one updated reference file in `.claude/skills/build-presentation/references/`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-vocabulary/02-CONTEXT.md
@new_insights/plans/02-visual-vocabulary-stylist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create visual-vocabulary.md reference</name>
  <files>
    .claude/skills/build-presentation/references/visual-vocabulary.md
  </files>
  <read_first>
    new_insights/plans/02-visual-vocabulary-stylist.md
    .planning/phases/02-visual-vocabulary/02-CONTEXT.md
    .claude/skills/build-presentation/references/component-catalog.md
    tokens/visuals.css
  </read_first>
  <action>
    Create `.claude/skills/build-presentation/references/visual-vocabulary.md`. Target: 200-280 lines.

    **Structure:**

    H1: "Visual Vocabulary" with brief intro (what this file is, how strategist uses it — hard mapping, not optional)

    H2: "The 15 Content Archetypes"
    - Quick-reference table with columns: #, Archetype, Detection Signals (content triggers), Primary Component, Fallback Component
    - Table must cover all 15 archetypes from 02-visual-vocabulary-stylist.md plan

    H2: "Archetype Reference" — one H3 per archetype (15 total), each containing:
    - **Archetype name** as H3 heading (machine-readable slug in parentheses, e.g. `list-of-items`)
    - **Detection signals:** 3-5 content patterns that trigger this archetype (e.g. "3+ parallel items with no inherent order")
    - **Primary component:** component name + specific visual treatment
    - **Fallback component:** alternative if primary doesn't fit
    - **Visual treatment notes:** how to visually emphasize the content type (e.g. icon+label layout, traffic light color coding)
    - **`--comp-*` hints:** 1-2 relevant CSS variables that the stylist might tweak for this archetype

    H2: "Bullet-List Smell Test"
    - Decision tree the strategist applies BEFORE defaulting to text-heavy component
    - If content has 3+ items, run through the tree: ordered? → timeline. Numbers? → metrics. Categories? → card-grid with icons. Comparisons? → comparison or data-table. Statuses? → traffic lights or Harvey balls. Short labels? → icon+label pairs. None of the above? → text-heavy is OK, but add a lead visual.
    - Written as a numbered decision tree (not prose), each node is a yes/no question with a clear routing outcome

    H2: "Lucide Icon Set (Consulting Curated)"
    - Intro: Icons are inline SVG. Builder copies SVG source directly from lucide.dev. No CDN.
    - Table with 10 domains, each domain listing 5 Lucide icon names:
      - Strategy: target, compass, map, telescope, crosshair
      - Growth: trending-up, chart-bar, chart-line, arrow-up-right, activity
      - People: users, user-check, handshake, building-2, award
      - Technology: cpu, cloud, database, code-2, server
      - Process: workflow, git-branch, repeat, layers, list-checks
      - Finance: wallet, banknote, credit-card, receipt, piggy-bank
      - Communication: mail, phone, globe, megaphone, message-circle
      - Status: check-circle, alert-triangle, x-circle, clock, shield-check
      - Innovation: lightbulb, rocket, sparkles, zap, brain
      - Navigation: arrow-right, chevrons-right, move-right, corner-down-right, external-link

    H2: "Deck-Plan Integration"
    - Show the exact deck-plan.md field format that strategist adds per content slide:
      ```
      - **Content archetype:** list-of-items
      - **Visual treatment:** icon+label cards (Lucide: target, trending-up, users)
      ```
    - Show the validation warning format for slides that should use a richer visual:
      ```
      > **Visual Treatment Audit:**
      > - Slide 7 uses text-heavy with 4 sequential items → Consider: timeline
      ```

    The 15 archetypes (in order, use exact slugs from the plan document):
    1. list-of-items — Icon+label grid, card-grid
    2. sequential-process — Timeline, chevron flow
    3. comparison — Comparison cards, data-table
    4. hierarchy — Mermaid org chart, nested cards
    5. quantity-metric — Metrics, big number + trend arrow
    6. proportion — Doughnut/pie chart, stacked bar
    7. trend — Line chart, sparkline
    8. status-readiness — Traffic lights, Harvey balls
    9. categories — Color-coded cards, badge row
    10. cause-effect — Arrow chain, mermaid flow
    11. ranking — Horizontal bars, numbered list with emphasis
    12. matrix-positioning — Framework 2x2, bubble chart
    13. definition-concept — Quote component, callout + two-column
    14. evidence-proof — Quote + attribution, metric + sparkline
    15. recommendation — Summary, numbered action cards
  </action>
  <verify>
    <automated>test -f .claude/skills/build-presentation/references/visual-vocabulary.md && grep -c "^### " .claude/skills/build-presentation/references/visual-vocabulary.md && wc -l .claude/skills/build-presentation/references/visual-vocabulary.md</automated>
  </verify>
  <acceptance_criteria>
    - visual-vocabulary.md exists and is 200-300 lines
    - File contains exactly 15 H3 archetype sections
    - Each archetype has Detection signals, Primary component, Fallback component
    - Bullet-list smell test section exists with decision tree
    - Lucide icon section exists with 10 domains x 5 icons
    - Deck-plan integration section shows the exact field format
  </acceptance_criteria>
  <done>visual-vocabulary.md created with 15 archetypes, smell test, icon set, and deck-plan integration guide</done>
</task>

<task type="auto">
  <name>Task 2: Extend component-catalog.md with archetype cross-references</name>
  <files>
    .claude/skills/build-presentation/references/component-catalog.md
  </files>
  <read_first>
    .claude/skills/build-presentation/references/component-catalog.md
    .claude/skills/build-presentation/references/visual-vocabulary.md
  </read_first>
  <action>
    Add an "Archetypes" field to each of the 21 component sections in component-catalog.md.

    For each component section, add a line after "Audience fit":
    - **Archetypes:** [comma-separated list of archetype slugs this component is primary or fallback for]

    Archetype assignment per component:
    - title: recommendation, definition-concept
    - section-break: (none — structural slide, not content archetype)
    - text-heavy: definition-concept, evidence-proof (fallback for list-of-items when none of the above)
    - two-column: comparison, definition-concept, evidence-proof
    - metrics: quantity-metric (primary), trend (fallback)
    - image-full-bleed: definition-concept (emotional/aspirational), recommendation (closing impact)
    - agenda: sequential-process (fallback), list-of-items (agenda items)
    - summary: recommendation (primary)
    - contact: recommendation (closing CTA)
    - comparison: comparison (primary)
    - timeline: sequential-process (primary)
    - quote: evidence-proof (primary), definition-concept (fallback)
    - card-grid: list-of-items (primary), categories (primary)
    - framework: matrix-positioning (primary), categories (secondary)
    - data-table: comparison (primary), ranking (primary), status-readiness (fallback)
    - harvey-balls: status-readiness (primary)
    - chart: proportion (primary), trend (primary), ranking (primary), quantity-metric (fallback)
    - mermaid-diagram: hierarchy (primary), sequential-process (primary), cause-effect (primary)
    - waterfall: trend (primary), quantity-metric (fallback)
    - code-block: definition-concept (technical explanation)
    - team: list-of-items (people variant), categories (by role)

    Also add a one-line note to the Quick Reference table header (not each row, just the table intro) explaining that the catalog is extended with archetype cross-references from visual-vocabulary.md.

    Keep the file under 420 lines total.
  </action>
  <verify>
    <automated>grep -c "Archetypes:" .claude/skills/build-presentation/references/component-catalog.md && wc -l .claude/skills/build-presentation/references/component-catalog.md</automated>
  </verify>
  <acceptance_criteria>
    - component-catalog.md has at least 20 "Archetypes:" entries (21 components, section-break has none)
    - File stays under 430 lines
    - Each Archetypes entry references valid archetype slugs from visual-vocabulary.md
    - Existing content is preserved intact (Use when, Required slots, etc.)
  </acceptance_criteria>
  <done>Component catalog extended with archetype cross-references for all 21 components</done>
</task>

</tasks>

<verification>
- visual-vocabulary.md exists with 200-300 lines
- 15 H3 archetype sections with detection signals, primary/fallback components
- Bullet-list smell test decision tree present
- Lucide icon set curated (10 domains, 5 icons each)
- component-catalog.md has Archetypes field on 20+ components
- component-catalog.md stays under 430 lines
</verification>

<success_criteria>
A strategist agent reading visual-vocabulary.md can classify any content type into an archetype within one lookup, know which component to use and why, and know when NOT to default to a bullet list. The component catalog cross-references confirm the archetype→component mapping is bidirectional.
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-vocabulary/02-01-SUMMARY.md`
</output>
