---
phase: 08-pure-css-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/data-table.html
  - templates/harvey-balls.html
  - templates/metrics.html
autonomous: true
requirements: [VIZ-03, VIZ-05, VIZ-06]

must_haves:
  truths:
    - "Data table renders 2-6 columns and 3-12 rows with header styling, alternating row colors, and right-aligned numbers"
    - "Data table has two variants: minimal (thin lines, no row backgrounds) and structured (alternating rows, bold header)"
    - "Harvey balls display 5 states (0/25/50/75/100%) as pure CSS circles that render inline"
    - "Sparkline micro-charts (trend line, mini bar, progress) render inside metrics cards without JavaScript"
  artifacts:
    - path: "templates/data-table.html"
      provides: "Data table component with minimal and structured variants"
      contains: "comp-data-table"
    - path: "templates/harvey-balls.html"
      provides: "Harvey ball utility CSS and usage documentation"
      contains: "harvey-ball"
    - path: "templates/metrics.html"
      provides: "Enhanced metrics component with sparkline micro-charts"
      contains: "comp-metrics__spark"
  key_links:
    - from: "templates/data-table.html"
      to: "tokens/base.css"
      via: "var(--color-*) and var(--spacing-*) references"
      pattern: "var\\(--color-"
    - from: "templates/harvey-balls.html"
      to: "tokens/base.css"
      via: "var(--color-accent) for filled state"
      pattern: "var\\(--color-"
    - from: "templates/metrics.html"
      to: "tokens/base.css"
      via: "var(--color-chart-*) for sparkline colors"
      pattern: "var\\(--color-chart"
---

<objective>
Create three pure CSS data visualization components: a data table with two visual variants (minimal and structured), Harvey ball capability-rating dots, and sparkline micro-charts that enhance the existing metrics component.

Purpose: These components bring consulting-grade data presentation capabilities to the framework without any JavaScript dependencies.
Output: Three template files (one new, one new utility, one enhanced existing).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pure-css-components/08-CONTEXT.md

@templates/metrics.html
@templates/comparison.html
@templates/contact.html
@tokens/base.css

<interfaces>
<!-- Existing design token system (22 color tokens) -->
From tokens/base.css:
```css
--color-primary, --color-secondary, --color-accent
--color-background, --color-surface, --color-text, --color-text-muted
--color-border, --color-success, --color-danger
--color-highlight, --color-callout
--color-chart-1 through --color-chart-5
--color-warning, --color-info, --color-on-primary
--spacing-xs, --spacing-sm, --spacing-md, --spacing-lg, --spacing-xl, --spacing-2xl, --spacing-3xl
--font-size-display, --font-size-heading, --font-size-subheading, --font-size-body, --font-size-caption
--radius-sm, --radius-md, --radius-lg, --radius-full
--shadow-card, --shadow-md
```

From templates/metrics.html (existing structure):
```html
<section data-component="metrics">
  <div class="comp-metrics">
    <h2 class="comp-metrics__title">...</h2>
    <div class="comp-metrics__grid">
      <div class="comp-metrics__card">
        <span class="comp-metrics__number">+23%</span>
        <span class="comp-metrics__label">Label</span>
      </div>
    </div>
  </div>
</section>
```

Established component patterns:
- BEM-lite: comp-{name}, comp-{name}__{element}, comp-{name}--{variant}
- Comment header: COMPONENT, USE WHEN, REQUIRED/OPTIONAL SLOTS, MASTER LAYER, LAYOUT NOTES
- Token-aware: all colors via var(--color-*), spacing via var(--spacing-*)
- German safe: overflow-wrap: break-word, hyphens: auto
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data table component and Harvey ball utility</name>
  <files>templates/data-table.html, templates/harvey-balls.html</files>
  <read_first>templates/comparison.html, templates/card-grid.html, tokens/base.css</read_first>
  <action>
Create `templates/data-table.html` (COMP-15 / VIZ-03) following the established template pattern:

**Comment header:** COMPONENT: Data Table (COMP-15), USE WHEN: Financial data, feature comparisons, status matrices, vendor evaluations. REQUIRED SLOTS: thead row with th cells, at least 1 tbody row. OPTIONAL SLOTS: slide title (h2), summary/total row (tfoot), column highlighting class. MASTER LAYER: Visible. LAYOUT NOTES: Two variants — minimal and structured.

**CSS (embedded style block):**
- `.comp-data-table` — flex column, full height, standard padding (var(--spacing-2xl) var(--spacing-3xl)), overflow-wrap: break-word, hyphens: auto
- `.comp-data-table h2` — heading styles matching other components (font-size: var(--font-size-heading), color: var(--color-primary), margin-bottom: var(--spacing-xl))
- `.comp-data-table table` — width: 100%, border-collapse: collapse, font-size: var(--font-size-body), font-variant-numeric: tabular-nums
- `.comp-data-table th` — text-align: left, padding: var(--spacing-sm) var(--spacing-md), font-weight: var(--font-weight-semibold), color: var(--color-primary), border-bottom: 2px solid var(--color-border)
- `.comp-data-table td` — padding: var(--spacing-sm) var(--spacing-md), border-bottom: 1px solid var(--color-border), color: var(--color-text)
- `.comp-data-table__num` (class for numeric cells) — text-align: right, font-variant-numeric: tabular-nums
- `.comp-data-table tfoot td` — font-weight: var(--font-weight-bold), border-top: 2px solid var(--color-primary), color: var(--color-primary)
- `.comp-data-table--highlight td:nth-child(N)` — use a highlight column utility class pattern

**Structured variant** (default, per D-01):
- `.comp-data-table--structured th` — background: var(--color-primary), color: var(--color-on-primary), font-weight: var(--font-weight-bold)
- `.comp-data-table--structured tbody tr:nth-child(even)` — background: var(--color-surface)

**Minimal variant** (per D-01):
- `.comp-data-table--minimal th` — background: transparent, color: var(--color-primary), border-bottom: 1px solid var(--color-border), font-weight: var(--font-weight-medium)
- `.comp-data-table--minimal td` — border-bottom: 1px solid rgba from var(--color-border) at 0.5 opacity (use color-mix or just lighter border)
- No alternating row colors

**HTML example:** A 4-column, 5-row German financial table using the structured variant. Columns: Geschaeftsbereich, Umsatz (Mio. EUR), Wachstum (%), Marge (%). Include proper German number formatting (comma decimals: 12,4 not 12.4). Add a tfoot summary row. Right-align all numeric columns with comp-data-table__num class.

Then create `templates/harvey-balls.html` (VIZ-05) as a utility/reference component:

**Comment header:** COMPONENT: Harvey Balls (Utility), USE WHEN: Capability ratings in tables, feature comparisons, maturity assessments. Inside data tables, comparison cards, list items. REQUIRED: One of 5 state classes. OPTIONAL: Color-coded variant. LAYOUT NOTES: Inline element, 16px default size, scales with font-size.

**CSS:**
- `.harvey-ball` — display: inline-block, width: 1em, height: 1em, border-radius: var(--radius-full), border: 2px solid var(--color-accent), position: relative, vertical-align: middle
- `.harvey-ball--0` — background: transparent (empty)
- `.harvey-ball--25` — background: conic-gradient(var(--color-accent) 0deg 90deg, transparent 90deg 360deg)
- `.harvey-ball--50` — background: conic-gradient(var(--color-accent) 0deg 180deg, transparent 180deg 360deg)
- `.harvey-ball--75` — background: conic-gradient(var(--color-accent) 0deg 270deg, transparent 270deg 360deg)
- `.harvey-ball--100` — background: var(--color-accent) (fully filled)
- Size utility: `.harvey-ball--lg` — width: 1.5em, height: 1.5em (for standalone use)

**Color-coded variant** (per D-04):
- `.harvey-ball--color.harvey-ball--100` — border-color and fill: var(--color-success)
- `.harvey-ball--color.harvey-ball--75` — border-color and fill: var(--color-chart-2)
- `.harvey-ball--color.harvey-ball--50` — border-color and fill: var(--color-warning)
- `.harvey-ball--color.harvey-ball--25` — border-color and fill: var(--color-chart-4)
- `.harvey-ball--color.harvey-ball--0` — border-color: var(--color-danger)

**HTML example:** Show Harvey balls in a standalone demo section, then show them embedded in a mini comparison table (Feature | Anbieter A | Anbieter B) with German labels. Use data-component="harvey-balls" for the standalone demo and show inline usage documentation in the comment header.
  </action>
  <verify>
    <automated>test -f templates/data-table.html && test -f templates/harvey-balls.html && grep -q "comp-data-table" templates/data-table.html && grep -q "harvey-ball--50" templates/harvey-balls.html && grep -q "conic-gradient" templates/harvey-balls.html && grep -q "comp-data-table--minimal" templates/data-table.html && grep -q "comp-data-table--structured" templates/data-table.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- templates/data-table.html exists with BEM-lite classes, comment header, embedded CSS, and German financial example
- Two variants: comp-data-table--structured (alternating rows, bold header) and comp-data-table--minimal (thin lines, no backgrounds)
- Right-aligned numbers via comp-data-table__num class, tabular-nums enabled
- Summary/total row via tfoot with bold styling and top border
- templates/harvey-balls.html exists with 5 states (0/25/50/75/100%) via conic-gradient
- Harvey balls render inline (1em sizing), with optional color-coded variant
- All colors use design token references, no hardcoded values
- German text (umlauts, comma decimals) renders correctly in examples
  </acceptance_criteria>
  <done>Data table component renders with structured and minimal variants, Harvey balls display 5 fill states as pure CSS circles</done>
</task>

<task type="auto">
  <name>Task 2: Add sparkline micro-charts to metrics component</name>
  <files>templates/metrics.html</files>
  <read_first>templates/metrics.html, tokens/base.css</read_first>
  <action>
Enhance `templates/metrics.html` to support optional sparkline/micro-chart indicators (VIZ-06, per D-07, D-08, D-09). This enhances the existing component — not a standalone component.

**Add to the existing style block** (do NOT remove or change any existing CSS rules):

Trend indicator (up/down/flat arrow):
- `.comp-metrics__trend` — display: inline-flex, align-items: center, gap: var(--spacing-xs), font-size: var(--font-size-caption), margin-top: var(--spacing-xs)
- `.comp-metrics__trend--up` — color: var(--color-success)
- `.comp-metrics__trend--down` — color: var(--color-danger)
- `.comp-metrics__trend--flat` — color: var(--color-text-muted)
- `.comp-metrics__trend-arrow` — display: inline-block (uses inline SVG arrow or CSS triangle)

Mini bar chart (3-7 bars showing relative values):
- `.comp-metrics__bars` — display: flex, align-items: flex-end, gap: 2px, height: 32px, margin-top: var(--spacing-sm)
- `.comp-metrics__bar` — width: 6px, border-radius: 2px 2px 0 0, background: var(--color-chart-1), min-height: 3px
- Height is set via inline style (style="height: 80%") on each bar element

Progress indicator (horizontal bar):
- `.comp-metrics__progress` — width: 100%, height: 6px, background: var(--color-border), border-radius: var(--radius-full), margin-top: var(--spacing-sm), overflow: hidden
- `.comp-metrics__progress-fill` — height: 100%, border-radius: var(--radius-full), background: var(--color-accent), transition: none (no JS needed)
- Width is set via inline style (style="width: 73%")

**Update comment header:** Add sparkline slots to OPTIONAL SLOTS section:
- Trend indicator (up/down/flat with percentage)
- Mini bar chart (3-7 bars)
- Progress bar (percentage fill)

**Update HTML example:** Keep the existing 3-metric example but enhance each card with a different sparkline type:
1. First metric (+23% Umsatzwachstum): Add trend-up indicator with small green arrow SVG and "+5pp vs. Vorjahr" text
2. Second metric (4.2M Neue Nutzer): Add mini bar chart with 6 bars showing monthly progression (heights: 40%, 55%, 45%, 65%, 75%, 80%)
3. Third metric (98.7% Verfuegbarkeit): Add progress bar filled to ~99%

Use inline SVG for the trend arrows (simple chevron-up/chevron-down paths, 12x12px, stroke: currentColor). Do NOT use emoji or HTML entities.
  </action>
  <verify>
    <automated>grep -q "comp-metrics__trend" templates/metrics.html && grep -q "comp-metrics__bars" templates/metrics.html && grep -q "comp-metrics__progress" templates/metrics.html && grep -q "comp-metrics__bar" templates/metrics.html && grep -q "color-success" templates/metrics.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- metrics.html retains ALL existing CSS and HTML structure unchanged
- Three new sparkline types added: trend indicator, mini bar chart, progress bar
- Trend indicator shows colored arrow + delta text (green up, red down, grey flat)
- Mini bar chart renders 3-7 bars at varying heights using inline styles
- Progress bar renders a horizontal fill using inline width style
- All new CSS uses design token references (no hardcoded colors)
- Comment header updated with sparkline slot documentation
- HTML example shows all three sparkline types in the 3-metric demo
  </acceptance_criteria>
  <done>Metrics component supports optional sparkline micro-charts (trend, bars, progress) rendering inside metric cards without JavaScript</done>
</task>

</tasks>

<verification>
Open a test HTML file in a browser that includes all three templates:
1. Data table with structured variant shows alternating rows, bold header, right-aligned numbers
2. Data table with minimal variant shows thin lines only, no row backgrounds
3. Harvey balls display 5 fill states (0/25/50/75/100%) as circles with conic-gradient fills
4. Metrics cards show trend arrows, mini bars, and progress indicators
5. All components use design tokens and render correctly with German text
</verification>

<success_criteria>
- 2 new template files created (data-table.html, harvey-balls.html)
- 1 existing template enhanced (metrics.html with sparklines)
- All CSS is pure — no JavaScript dependencies
- German number formatting (comma decimals) used in examples
- All colors reference design tokens
</success_criteria>

<output>
After completion, create `.planning/phases/08-pure-css-components/08-01-SUMMARY.md`
</output>
