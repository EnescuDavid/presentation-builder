---
phase: 06-review-pipeline
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - .claude/skills/build-presentation/workflows/build-new-deck.md
  - .claude/skills/build-presentation/workflows/refine-deck.md
autonomous: true
requirements:
  - REVIEW-03
  - REVIEW-04
must_haves:
  truths:
    - "build-new-deck.md creates build-log.yaml in Step 1 with meta block"
    - "build-new-deck.md logs phase transitions to build-log.yaml"
    - "build-new-deck.md updates summary block in Step 9 (Delivery)"
    - "build-new-deck.md Step 8 review integration uses [BLOCKER-N] tag parsing"
    - "refine-deck.md initializes build-log.yaml if missing and supports quick review mode"
  artifacts:
    - path: ".claude/skills/build-presentation/workflows/build-new-deck.md"
      provides: "Build-log initialization, phase transition logging, summary update, review integration"
      contains: "build-log.yaml"
    - path: ".claude/skills/build-presentation/workflows/refine-deck.md"
      provides: "Build-log initialization guard and quick review mode"
      contains: "build-log.yaml"
  key_links:
    - from: ".claude/skills/build-presentation/workflows/build-new-deck.md"
      to: "projects/{name}/.pipeline/build-log.yaml"
      via: "cat > (init) and cat >> (append)"
      pattern: "build-log.yaml"
    - from: ".claude/skills/build-presentation/workflows/build-new-deck.md"
      to: ".claude/agents/presentation-reviewer.md"
      via: "subagent spawn in Step 8"
      pattern: "presentation-reviewer"
    - from: ".claude/skills/build-presentation/workflows/refine-deck.md"
      to: "projects/{name}/.pipeline/build-log.yaml"
      via: "guard init + append"
      pattern: "build-log.yaml"
---

<objective>
Update the two orchestrator workflows to integrate build-log initialization/logging and strengthen the review pipeline integration. Per D-06, the orchestrator logs phase transitions and user decisions. Per D-09, the review auto-fix loop is already in Step 8 -- this plan ensures the review report format and build-log are properly connected.

Purpose: Complete the traceability chain by having the orchestrator create and maintain build-log.yaml, and ensure the review integration in build-new-deck.md properly parses the reviewer's machine-parseable output.
Output: Updated build-new-deck.md and refine-deck.md workflows
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-review-pipeline/06-CONTEXT.md
@.planning/phases/06-review-pipeline/06-RESEARCH.md

<interfaces>
<!-- Build-log format (from Plan 01) -->
Initialization template:
```yaml
meta:
  project: "{name}"
  started: "{ISO-8601}"
  mode: "normal"

entries: []

summary:
  status: "in-progress"
  total_duration_s: 0
  pipeline_flow: "started"
```

Summary update format:
```yaml
summary:
  status: "success|failure"
  total_duration_s: {number}
  pipeline_flow: "{brief(OK) -> research(N blocks) -> strategy(N slides) -> review(pass) -> build(done)}"
```

<!-- Reviewer output format (from Plan 02) -->
Review report header lines:
```
**Story:** PASS|FAIL ({N} blockers, {M} warnings)
**Visual:** PASS|FAIL ({N} blockers, {M} warnings)
```

Blocker format:
```yaml
[BLOCKER-1] {stage}: {check ID}: {description}
  - Slide: {N}
  - Fix: {required action}
```

<!-- Existing workflow structure -->
build-new-deck.md: Entry (resumability check) -> Step 1-9
refine-deck.md: Entry -> classify change -> route to agent -> quick review
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add build-log initialization and phase logging to build-new-deck.md</name>
  <files>.claude/skills/build-presentation/workflows/build-new-deck.md</files>
  <read_first>
    - .claude/skills/build-presentation/workflows/build-new-deck.md (full file -- understand current Step 1 through Step 9 structure)
    - references/build-log-format.md (initialization template, append pattern, summary update format)
    - .planning/phases/06-review-pipeline/06-RESEARCH.md (Pattern 2: review report format, Pattern 1: build-log append, open questions about summary block)
  </read_first>
  <action>
Make these targeted additions to `build-new-deck.md`. Do NOT rewrite the entire file -- surgically add/modify specific sections.

**1. Step 1 addition -- build-log initialization (per D-06):**

After the existing Step 1 content (brief intake), add a substep that creates the build-log file. Insert after the `mkdir -p projects/{name}/.pipeline` line (or add that line if it doesn't exist):

```markdown
### 1c: Initialize Build Log

Create the pipeline trace file:
```bash
cat > projects/{name}/.pipeline/build-log.yaml << 'EOF'
meta:
  project: "{name}"
  started: "{ISO-8601-timestamp}"
  mode: "normal"

entries: []

summary:
  status: "in-progress"
  total_duration_s: 0
  pipeline_flow: "started"
EOF
```

All subsequent agents append to this file. See `references/build-log-format.md` for schema.
```yaml

**2. Phase transition logging (per D-06):**

After each agent spawn step (Steps 2-8), add a build-log append instruction. Insert after each "Wait for completion" line:

```markdown
Log the phase transition:
```bash
cat >> projects/{name}/.pipeline/build-log.yaml << 'ENTRY'
  - ts: "{timestamp}"
    agent: "orchestrator"
    phase: "{step-label}"
    event: "phase_end"
    message: "{step} complete"
    verbose_only: false
ENTRY
```

```

The specific step labels and messages are:
- Step 2 (research): phase "research", message "Research complete"
- Step 3 (brand check): phase "brand-check", message "Brand check complete"
- Step 4 (debate): phase "debate", message "Debate resolved after {N} rounds"
- Step 5 (user review): phase "user-review", event "user_action", message "User approved deck-plan"
- Step 6 (build): phase "build", message "Build complete"
- Step 7 (stylist): phase "stylist", message "Stylist pass complete"
- Step 8 (review): phase "review", message "Review complete: Story {PASS|FAIL}, Visual {PASS|FAIL}"

Keep these as brief inline instructions (1-3 lines each), not full code blocks. The orchestrator is a workflow guide, not a script.

**3. Step 8 strengthening -- review report parsing (per D-09):**

The current Step 8 already has the auto-fix loop structure. Add explicit parsing instructions for the reviewer's machine-parseable output:

After "Read `.pipeline/review-report.md`", add:
```markdown
Parse the review scorecard:
- Read the `**Story:**` line -- extract PASS or FAIL
- Read the `**Visual:**` line -- extract PASS or FAIL
- If either shows FAIL, read the `## Blockers` section
- Count `[BLOCKER-N]` tags -- these are the findings to fix
- Surface each blocker to the user with its slide number and fix recommendation
```

**4. Step 9 addition -- build-log summary update:**

At the end of Step 9 (Delivery), add:

```markdown
### 9b: Finalize Build Log

Update the summary block in build-log.yaml:
```bash
# Read the file, update the summary section
python3 -c "
import yaml
with open('projects/{name}/.pipeline/build-log.yaml') as f:
    log = yaml.safe_load(f)
log['summary'] = {
    'status': 'success',
    'total_duration_s': 0,
    'pipeline_flow': '{brief(OK) -> research -> strategy -> review -> build -> delivery}'
}
with open('projects/{name}/.pipeline/build-log.yaml', 'w') as f:
    yaml.dump(log, f, default_flow_style=False, sort_keys=False, allow_unicode=True)
"
```

Fill in the actual pipeline_flow string from the steps that ran (skip steps that were skipped).
```yaml

**5. Required reading update:**

Add `references/build-log-format.md` to the `<required_reading>` block at the top of the file.
  </action>
  <verify>
    <automated>grep -c "build-log" .claude/skills/build-presentation/workflows/build-new-deck.md</automated>
  </verify>
  <acceptance_criteria>
    - build-new-deck.md contains "build-log.yaml" in Step 1 (initialization)
    - build-new-deck.md contains `cat >` for build-log creation in Step 1
    - build-new-deck.md contains "BLOCKER" tag parsing instructions in Step 8
    - build-new-deck.md contains summary update in Step 9
    - build-new-deck.md contains `references/build-log-format.md` in required_reading
    - build-new-deck.md mentions "build-log" at least 8 times (init + 6 phase transitions + summary)
    - Step 8 contains "Story" and "Visual" and "PASS" and "FAIL" parsing instructions
  </acceptance_criteria>
  <done>build-new-deck.md creates build-log.yaml at pipeline start, logs phase transitions throughout, parses reviewer output with [BLOCKER-N] tags, and updates summary at delivery</done>
</task>

<task type="auto">
  <name>Task 2: Add build-log guard and quick review mode to refine-deck.md</name>
  <files>.claude/skills/build-presentation/workflows/refine-deck.md</files>
  <read_first>
    - .claude/skills/build-presentation/workflows/refine-deck.md (full file -- understand current structure and where to insert)
    - references/build-log-format.md (guard pattern for missing build-log)
    - new_insights/plans/04-review-pipeline.md (Quick Review Mode section -- triggered for changes affecting <=3 slides)
  </read_first>
  <action>
Make targeted additions to `refine-deck.md`. Do NOT rewrite the entire file.

**1. Build-log guard at entry:**

At the start of the workflow (after initial reading/parsing), add:

```markdown
### Build Log Guard

Ensure build-log.yaml exists for traceability:
```bash
mkdir -p projects/{name}/.pipeline
[ -f projects/{name}/.pipeline/build-log.yaml ] || cat > projects/{name}/.pipeline/build-log.yaml << 'INIT'
meta:
  project: "{name}"
  started: "unknown"
  mode: "normal"

entries: []

summary:
  status: "in-progress"
  total_duration_s: 0
  pipeline_flow: "refine-invocation"
INIT
```

```yaml

**2. Build-log append after each agent dispatch:**

After each agent is dispatched (stylist, editor, builder, etc.), add:

```markdown
Log the refinement:
```bash
cat >> projects/{name}/.pipeline/build-log.yaml << 'ENTRY'
  - ts: "{timestamp}"
    agent: "orchestrator"
    phase: "refine"
    event: "decision"
    message: "Refinement: {change description} via {agent-name}"
    verbose_only: false
ENTRY
```

```

**3. Quick review mode integration:**

Add a section near the end of the workflow (after the agent completes its work):

```markdown
### Quick Review (for changes affecting <=3 slides)

After the agent completes:
1. Count how many slides were affected by the change
2. If <= 3 slides changed:
   - Run only the checks relevant to the change type:
     - Content change (editor) -> Story checks S1-S3, S7, S10 only
     - CSS change (stylist) -> Visual checks V1-V3, V6, V9, V13 only
     - Full rebuild (builder) -> Run full reviewer
   - Report findings inline in conversation (no review-report.md file needed)
   - No screenshots unless explicitly requested
3. If > 3 slides changed:
   - Run the full presentation-reviewer agent (same as build-new-deck Step 8)
   - Write .pipeline/review-report.md
```

**4. Required reading update:**

Add `references/build-log-format.md` to the required_reading block.
  </action>
  <verify>
    <automated>grep -c "build-log" .claude/skills/build-presentation/workflows/refine-deck.md</automated>
  </verify>
  <acceptance_criteria>
    - refine-deck.md contains "build-log.yaml" (guard initialization)
    - refine-deck.md contains the `[ -f ... ] ||` guard pattern
    - refine-deck.md contains "Quick Review" section
    - refine-deck.md mentions "<= 3 slides" or "<=3 slides" for quick review threshold
    - refine-deck.md contains `references/build-log-format.md` in required_reading or context
    - refine-deck.md mentions build-log at least 3 times
  </acceptance_criteria>
  <done>refine-deck.md initializes build-log.yaml when missing, logs refinement decisions, and supports quick review mode for small changes (<=3 slides) with targeted checks</done>
</task>

</tasks>

<verification>
- `grep -c "build-log" .claude/skills/build-presentation/workflows/build-new-deck.md` shows >= 8
- `grep -c "build-log" .claude/skills/build-presentation/workflows/refine-deck.md` shows >= 3
- `grep "BLOCKER" .claude/skills/build-presentation/workflows/build-new-deck.md | head -5` shows parsing instructions
- `grep "Quick Review" .claude/skills/build-presentation/workflows/refine-deck.md` shows quick review section
</verification>

<success_criteria>
- build-new-deck.md has complete build-log lifecycle (init -> transitions -> summary)
- build-new-deck.md Step 8 review integration parses [BLOCKER-N] tags per D-09
- refine-deck.md handles missing build-log gracefully
- refine-deck.md supports quick review mode for small changes
- Both workflows reference build-log-format.md
</success_criteria>

<output>
After completion, create `.planning/phases/06-review-pipeline/06-04-SUMMARY.md`
</output>
