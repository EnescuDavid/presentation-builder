---
phase: 06-review-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - .claude/agents/presentation-researcher.md
  - .claude/agents/brand-checker.md
  - .claude/agents/brand-profiler.md
  - .claude/agents/narrative-planner.md
  - .claude/agents/presentation-architect.md
  - .claude/agents/presentation-critic.md
  - .claude/agents/presentation-builder.md
  - .claude/agents/slide-stylist.md
  - .claude/agents/slide-editor.md
  - .claude/agents/presentation-reviewer.md
autonomous: true
requirements:
  - REVIEW-03
must_haves:
  truths:
    - "Every agent definition contains a build-log append step as its final workflow step"
    - "Each agent logs the correct event types per the taxonomy in build-log-format.md"
    - "Build-log append uses Bash cat-append with 2-space YAML indentation"
    - "All agents handle missing build-log.yaml gracefully with guard pattern"
  artifacts:
    - path: ".claude/agents/presentation-researcher.md"
      provides: "Build-log integration"
      contains: "build-log"
    - path: ".claude/agents/brand-checker.md"
      provides: "Build-log integration"
      contains: "build-log"
    - path: ".claude/agents/brand-profiler.md"
      provides: "Build-log integration"
      contains: "build-log"
    - path: ".claude/agents/narrative-planner.md"
      provides: "Build-log integration"
      contains: "build-log"
    - path: ".claude/agents/presentation-architect.md"
      provides: "Build-log integration"
      contains: "build-log"
    - path: ".claude/agents/presentation-critic.md"
      provides: "Build-log integration"
      contains: "build-log"
    - path: ".claude/agents/presentation-builder.md"
      provides: "Build-log integration"
      contains: "build-log"
    - path: ".claude/agents/slide-stylist.md"
      provides: "Build-log integration"
      contains: "build-log"
    - path: ".claude/agents/slide-editor.md"
      provides: "Build-log integration"
      contains: "build-log"
    - path: ".claude/agents/presentation-reviewer.md"
      provides: "Build-log integration"
      contains: "build-log"
  key_links:
    - from: ".claude/agents/*.md"
      to: "projects/{name}/.pipeline/build-log.yaml"
      via: "Bash cat-append"
      pattern: "build-log.yaml"
---

<objective>
Retrofit build-log append steps into all 10 agent definitions (9 existing + the new presentation-reviewer). Per D-03, this is a careful quality pass ensuring all agents are well-crafted and consistent. Per D-04, the agents are: presentation-researcher, brand-checker, brand-profiler, narrative-planner, presentation-architect, presentation-critic, presentation-builder, slide-stylist, slide-editor, plus presentation-reviewer.

Purpose: Per D-05, every pipeline step becomes traceable via build-log.yaml entries. Each agent appends phase_start, key events, and phase_end entries using the schema from references/build-log-format.md.
Output: All 10 agent .md files updated with build-log append steps
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-review-pipeline/06-CONTEXT.md
@.planning/phases/06-review-pipeline/06-RESEARCH.md

<interfaces>
<!-- Build-log format reference (created by Plan 01) -->
From references/build-log-format.md:
- Append pattern: Bash cat-append with 2-space indentation
- Guard: `[ -f projects/{name}/.pipeline/build-log.yaml ] || {initialize}`
- Entry fields: ts, agent, phase, event, message, verbose_only
- Event types: phase_start, phase_end, phase_skip, decision, validation, error, user_action, artifact_written

Per-agent event taxonomy:
| Agent | Events |
|-------|--------|
| presentation-researcher | phase_start, phase_end, artifact_written |
| brand-checker | phase_start, phase_end, artifact_written |
| brand-profiler | phase_start, phase_end, artifact_written |
| narrative-planner | phase_start, decision, phase_end, artifact_written |
| presentation-architect | phase_start, validation (per check), phase_end, artifact_written |
| presentation-critic | phase_start, validation (per check), phase_end, artifact_written |
| presentation-builder | phase_start, decision (deviations), artifact_written, phase_end |
| slide-stylist | phase_start, artifact_written, phase_end |
| slide-editor | phase_start, decision, artifact_written, phase_end |
| presentation-reviewer | phase_start, validation (per check), phase_end, artifact_written |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add build-log append steps to first 5 agents</name>
  <files>.claude/agents/presentation-researcher.md, .claude/agents/brand-checker.md, .claude/agents/brand-profiler.md, .claude/agents/narrative-planner.md, .claude/agents/presentation-architect.md</files>
  <read_first>
    - references/build-log-format.md (the canonical schema reference with append pattern, guard pattern, and per-agent event taxonomy)
    - .claude/agents/presentation-researcher.md (full file -- find last workflow step number, add new step after it)
    - .claude/agents/brand-checker.md (full file)
    - .claude/agents/brand-profiler.md (full file)
    - .claude/agents/narrative-planner.md (full file)
    - .claude/agents/presentation-architect.md (full file)
    - .planning/phases/06-review-pipeline/06-RESEARCH.md (Pattern 5: Build-Log Retrofit -- per-agent event taxonomy and append template)
  </read_first>
  <action>
For each of the 5 agents, add a new final workflow step titled "## Step N: Append Build Log" (where N is the next step number after the agent's existing last step). Also add `references/build-log-format.md` to the `<required_reading>` list of each agent.

The build-log step for each agent follows this template but with agent-specific events and messages:

```markdown
## Step {N}: Append Build Log

Append entries to `projects/{name}/.pipeline/build-log.yaml`.

Guard: if the file does not exist, create it first:
```bash
mkdir -p projects/{name}/.pipeline
[ -f projects/{name}/.pipeline/build-log.yaml ] || cat > projects/{name}/.pipeline/build-log.yaml << 'INIT'
meta:
  project: "{name}"
  started: "unknown"
  mode: "normal"

entries: []

summary:
  status: "in-progress"
  total_duration_s: 0
  pipeline_flow: "direct-invocation"
INIT
```

Append these entries using Bash cat-append (2-space indentation under entries:):
```bash
cat >> projects/{name}/.pipeline/build-log.yaml << 'ENTRY'
  - ts: "{timestamp}"
    agent: "{agent-name}"
    phase: "{phase-label}"
    event: "phase_start"
    message: "{start message}"
    verbose_only: false
  - ts: "{timestamp}"
    agent: "{agent-name}"
    phase: "{phase-label}"
    event: "{end-event}"
    message: "{end message}"
    verbose_only: false
ENTRY
```
```

**Per-agent specifics:**

**1. presentation-researcher.md:**
- Add to `<required_reading>`: `8. references/build-log-format.md -- build-log append pattern`
- phase label: `"research"`
- Events: phase_start ("Starting {mode} research for '{topic}'"), artifact_written ("research.md written -- {N} content blocks, {M} gaps flagged"), phase_end ("Research complete. Mode: {mode}")
- verbose_only: false for all (researcher has few events)

**2. brand-checker.md:**
- Add to `<required_reading>`: item for `references/build-log-format.md`
- phase label: `"brand-check"`
- Events: phase_start ("Checking brief against {brand} brand profile"), phase_end ("Brand check complete. Conflicts: {N}"), artifact_written ("brand-context.md written")
- verbose_only: false for all

**3. brand-profiler.md:**
- Add to `<required_reading>`: item for `references/build-log-format.md`
- phase label: `"brand-profile"`
- Events: phase_start ("Profiling brand from {N} input assets"), phase_end ("Brand profile complete"), artifact_written ("brand.yaml written" and "test-presentation.html written" -- two artifact entries)
- verbose_only: false for all

**4. narrative-planner.md:**
- Add to `<required_reading>`: item for `references/build-log-format.md`
- phase label: `"narrative-plan"`
- Events: phase_start ("Planning {audience} presentation: {topic}"), decision ("SCQA applied: {situation} -> {complication}" -- verbose_only: true), artifact_written ("round-{N}-plan.md written -- {slide_count} slides"), phase_end ("Narrative plan complete. Round {N}, {slide_count} slides")
- decision events are verbose_only: true; phase_start/end and artifact_written are false

**5. presentation-architect.md:**
- Add to `<required_reading>`: item for `references/build-log-format.md`
- phase label: `"architect-review"`
- Events: phase_start ("Evaluating round-{N}-plan.md structural quality"), validation entries per check ("Check {N} {name}: {BLOCKING|ADVISORY|PASS}" -- verbose_only: true), artifact_written ("round-{N}-architect.md written -- {blocking_count} BLOCKING, {advisory_count} ADVISORY"), phase_end ("Architect review complete. Verdict: {blocking_count} blocking, {advisory_count} advisory")
- Individual validation entries are verbose_only: true; summary events are false
  </action>
  <verify>
    <automated>for f in presentation-researcher brand-checker brand-profiler narrative-planner presentation-architect; do echo "$f:"; grep -c "build-log" .claude/agents/$f.md; done</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 agent files contains "build-log" (grep count >= 3 per file)
    - Each file's `<required_reading>` includes `references/build-log-format.md`
    - Each file has a step titled "Append Build Log" as its final workflow step
    - presentation-researcher.md contains `agent: "presentation-researcher"` and `phase: "research"`
    - brand-checker.md contains `agent: "brand-checker"` and `phase: "brand-check"`
    - brand-profiler.md contains `agent: "brand-profiler"` and `phase: "brand-profile"`
    - narrative-planner.md contains `agent: "narrative-planner"` and `phase: "narrative-plan"`
    - presentation-architect.md contains `agent: "presentation-architect"` and `phase: "architect-review"`
    - Each file contains the guard pattern `[ -f projects/{name}/.pipeline/build-log.yaml ]`
    - Each file uses Bash cat-append pattern (contains `cat >>`)
  </acceptance_criteria>
  <done>First 5 agents (researcher, brand-checker, brand-profiler, narrative-planner, architect) have build-log append steps with agent-specific events, guard patterns, and references/build-log-format.md in required_reading</done>
</task>

<task type="auto">
  <name>Task 2: Add build-log append steps to remaining 5 agents</name>
  <files>.claude/agents/presentation-critic.md, .claude/agents/presentation-builder.md, .claude/agents/slide-stylist.md, .claude/agents/slide-editor.md, .claude/agents/presentation-reviewer.md</files>
  <read_first>
    - references/build-log-format.md (canonical schema reference)
    - .claude/agents/presentation-critic.md (full file)
    - .claude/agents/presentation-builder.md (full file)
    - .claude/agents/slide-stylist.md (full file)
    - .claude/agents/slide-editor.md (full file)
    - .claude/agents/presentation-reviewer.md (full file -- created by Plan 02, verify it has build-log step; if it already has one from Plan 02, ensure it follows the same pattern as other agents; if not, add it)
  </read_first>
  <action>
Same pattern as Task 1 -- add build-log append step and required_reading entry to each agent.

**Per-agent specifics:**

**1. presentation-critic.md:**
- Add to `<required_reading>`: item for `references/build-log-format.md`
- phase label: `"critic-review"`
- Events: phase_start ("Evaluating round-{N}-plan.md argument quality"), validation entries per check ("Check {N} {name}: {BLOCKING|ADVISORY|PASS}" -- verbose_only: true), artifact_written ("round-{N}-critic.md written -- {blocking_count} BLOCKING, {advisory_count} ADVISORY"), phase_end ("Critic review complete. Verdict: {blocking_count} blocking, {advisory_count} advisory")
- Individual validation entries are verbose_only: true; summary events are false

**2. presentation-builder.md:**
- Add to `<required_reading>`: item for `references/build-log-format.md`
- phase label: `"build"`
- Events: phase_start ("Building {slide_count} slides from deck-plan.md"), decision entries per deviation from plan ("Deviation: {slide N} — {reason}" -- verbose_only: true), artifact_written per-slide progress ("Slide {N}/{total} built: {component}" -- verbose_only: true), artifact_written for final output ("presentation.html written -- {slide_count} slides, all class names verified" -- verbose_only: false), phase_end ("Build complete. Self-check: {N}/{total} passed." -- verbose_only: false)
- Per-slide progress entries are verbose_only: true

**3. slide-stylist.md:**
- Add to `<required_reading>`: item for `references/build-log-format.md`
- phase label: `"stylist"`
- Events: phase_start ("Applying CSS overrides to {N} slides"), artifact_written ("presentation.html updated -- {N} @layer overrides rules written"), phase_end ("Stylist pass complete")
- verbose_only: false for all (stylist has few events)

**4. slide-editor.md:**
- Add to `<required_reading>`: item for `references/build-log-format.md`
- phase label: `"edit"`
- Events: phase_start ("Editing slide {N}: {change_type}"), decision ("Change classified as: {tier}" -- verbose_only: false), artifact_written ("presentation.html updated -- {description}"), phase_end ("Edit complete")
- verbose_only: false for all

**5. presentation-reviewer.md:**
- Plan 02 already includes a build-log step (Step 6). Verify it exists and follows the same guard+append pattern as other agents. If it already has `references/build-log-format.md` in required_reading and uses the cat-append pattern with guard, no changes needed. If the pattern differs, align it to match the other agents exactly:
  - Guard pattern: `[ -f ... ] || cat > ...`
  - Append pattern: `cat >> ...`
  - phase label: `"review"`
  - Events per taxonomy table
  </action>
  <verify>
    <automated>for f in presentation-critic presentation-builder slide-stylist slide-editor presentation-reviewer; do echo "$f:"; grep -c "build-log" .claude/agents/$f.md; done</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 agent files contains "build-log" (grep count >= 3 per file)
    - Each file's `<required_reading>` includes `references/build-log-format.md`
    - Each file has a step titled "Append Build Log" as its final workflow step
    - presentation-critic.md contains `agent: "presentation-critic"` and `phase: "critic-review"`
    - presentation-builder.md contains `agent: "presentation-builder"` and `phase: "build"`
    - slide-stylist.md contains `agent: "slide-stylist"` and `phase: "stylist"`
    - slide-editor.md contains `agent: "slide-editor"` and `phase: "edit"`
    - presentation-reviewer.md contains `agent: "presentation-reviewer"` and `phase: "review"`
    - Each file contains the guard pattern `[ -f projects/{name}/.pipeline/build-log.yaml ]`
    - Each file uses Bash cat-append pattern (contains `cat >>`)
  </acceptance_criteria>
  <done>All 10 agents have consistent build-log append steps with agent-specific event types, guard patterns, and build-log-format.md references</done>
</task>

</tasks>

<verification>
- `for f in .claude/agents/*.md; do echo "$f:"; grep -c "build-log" "$f"; done` -- every agent file shows >= 3 matches
- `grep -l "build-log-format.md" .claude/agents/*.md | wc -l` -- shows 10
- `grep -l "cat >>" .claude/agents/*.md | wc -l` -- shows 10
</verification>

<success_criteria>
- All 10 agent definitions contain build-log append steps per D-03 and D-04
- Each agent uses the correct event types from the taxonomy per D-05
- All agents use consistent guard + cat-append pattern
- All agents reference build-log-format.md in required_reading
</success_criteria>

<output>
After completion, create `.planning/phases/06-review-pipeline/06-03-SUMMARY.md`
</output>
