---
phase: 06-review-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/check-contrast.js
  - tools/capture-slides.py
  - references/build-log-format.md
autonomous: true
requirements:
  - REVIEW-02
  - REVIEW-03
must_haves:
  truths:
    - "check-contrast.js reads brand CSS from brands/ directory (not themes/)"
    - "capture-slides.py produces per-slide PNGs, manifest.json, and contact-sheet.html"
    - "build-log-format.md documents the YAML schema with all 8 event types"
  artifacts:
    - path: "tools/capture-slides.py"
      provides: "Playwright screenshot capture for reveal.js presentations"
      min_lines: 80
    - path: "references/build-log-format.md"
      provides: "Build-log YAML schema reference for all agents"
      min_lines: 40
    - path: "tools/check-contrast.js"
      provides: "Fixed brand directory path"
      contains: "brands"
  key_links:
    - from: "tools/capture-slides.py"
      to: "projects/{name}/screenshots/"
      via: "Playwright headless Chromium"
      pattern: "sync_playwright"
    - from: "tools/check-contrast.js"
      to: "brands/"
      via: "themesDir constant"
      pattern: "path.join.*brands"
---

<objective>
Create the infrastructure foundations for the review pipeline: fix the check-contrast.js path bug (Phase 3 migrated themes/ to brands/), create the Playwright screenshot capture script, and write the build-log format reference document.

Purpose: These three artifacts are prerequisites or references for all subsequent plans -- the reviewer agent calls check-contrast.js and capture-slides.py, and all agents need the build-log format reference.
Output: Fixed tools/check-contrast.js, new tools/capture-slides.py, new references/build-log-format.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-review-pipeline/06-CONTEXT.md
@.planning/phases/06-review-pipeline/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix check-contrast.js path and create capture-slides.py</name>
  <files>tools/check-contrast.js, tools/capture-slides.py</files>
  <read_first>
    - tools/check-contrast.js (find the themesDir constant around line 132 -- must change 'themes' to 'brands')
    - tools/gallery.html (contact-sheet.html follows similar 3-column grid pattern)
    - .planning/phases/06-review-pipeline/06-RESEARCH.md (Pattern 3: capture-slides.py implementation with full code)
  </read_first>
  <action>
**1. Fix check-contrast.js (per D-07 prerequisite, Pitfall 1 from research):**

In `tools/check-contrast.js` around line 132, change:
```javascript
const themesDir = path.join(rootDir, 'themes');
```
to:
```javascript
const themesDir = path.join(rootDir, 'brands');
```

No other changes needed -- the theme discovery loop already handles subdirectories with theme.css.

**2. Create tools/capture-slides.py (per D-07):**

Create a Python script using Playwright sync_api + http.server to screenshot every slide of a reveal.js presentation. Implementation follows the research code example exactly:

- Shebang: `#!/usr/bin/env python3`
- Docstring with usage: `python3 tools/capture-slides.py projects/{name}/presentation.html`
- Imports: `sys, os, json, random, threading, pathlib, socket, http.server, argparse`
- `from playwright.sync_api import sync_playwright`
- `find_free_port(start=8100, end=8999)` -- uses `socket.connect_ex` probe on random sample
- `start_server(directory, port)` -- http.server.HTTPServer on localhost, daemon thread, log_message silenced
- `capture(html_path)` main function:
  - Resolve html_path to absolute, derive project_dir and out_dir (screenshots/)
  - Start local HTTP server serving project_dir
  - Launch headless Chromium at viewport 1280x720
  - `page.goto(url, wait_until='networkidle')`
  - `page.wait_for_function('() => typeof Reveal !== "undefined" && Reveal.isReady()')`
  - Get total slides via `Reveal.getTotalSlides()`
  - For each slide index i: `Reveal.slide(i)`, wait_for_timeout(150), detect overflow via `scrollWidth > clientWidth || scrollHeight > clientHeight` on all elements within `.present`, screenshot to `slide-{i+1:02d}.png`
  - Close browser, shutdown server
  - Write `manifest.json` with `{total: N, slides: [{index, file, overflows}]}`
  - Write `contact-sheet.html` with 3-column CSS grid of `<img>` tags, overflow warnings in figcaptions, Inter font, light grey background, white card shadows
- `--help` flag via argparse: description "Capture per-slide screenshots of a reveal.js presentation", positional arg "html_path"
- Exit 1 if no args

The contact-sheet.html must have `lang="de"`, title "Contact Sheet", and show overflow warnings with a warning icon character.
  </action>
  <verify>
    <automated>node tools/check-contrast.js 2>&1 | head -5 && python3 tools/capture-slides.py --help 2>&1 | head -3</automated>
  </verify>
  <acceptance_criteria>
    - tools/check-contrast.js contains `path.join(rootDir, 'brands')` (not 'themes')
    - tools/capture-slides.py exists and is >80 lines
    - `python3 tools/capture-slides.py --help` outputs usage text containing "presentation.html"
    - tools/capture-slides.py contains `from playwright.sync_api import sync_playwright`
    - tools/capture-slides.py contains `find_free_port`
    - tools/capture-slides.py contains `manifest.json`
    - tools/capture-slides.py contains `contact-sheet.html`
    - tools/capture-slides.py contains `Reveal.getTotalSlides()`
    - tools/capture-slides.py contains `scrollWidth > el.clientWidth`
  </acceptance_criteria>
  <done>check-contrast.js reads from brands/ directory; capture-slides.py exists with --help, Playwright screenshot capture, overflow detection, manifest.json, and contact-sheet.html generation</done>
</task>

<task type="auto">
  <name>Task 2: Create build-log-format.md reference document</name>
  <files>references/build-log-format.md</files>
  <read_first>
    - .planning/phases/06-review-pipeline/06-RESEARCH.md (Pattern 1: Agent Build-Log Append -- full YAML schema)
    - new_insights/plans/04-review-pipeline.md (Build Log section with format and agent logging taxonomy)
    - references/design-principles.md (reference doc style/formatting conventions to follow)
  </read_first>
  <action>
Create `references/build-log-format.md` as the canonical schema reference for build-log.yaml. Per D-05, the schema follows the spec from `new_insights/plans/04-review-pipeline.md`. This file is what agents reference when appending log entries.

Document structure:

```markdown
# Build Log Format Reference

> Schema reference for `projects/{name}/.pipeline/build-log.yaml`. Every pipeline agent appends entries to this file. The orchestrator creates it and writes the summary.

## File Location

`projects/{name}/.pipeline/build-log.yaml` -- created by `build-new-deck.md` Step 1, appended by all agents, summary updated by orchestrator at delivery.

## Schema

### Meta Block (written once by orchestrator)

```yaml
meta:
  project: "{project-name}"
  started: "{ISO-8601-timestamp}"
  mode: "normal"   # or "verbose"
```

### Entries List (appended by agents)

```yaml
entries:
  - ts: "{ISO-8601}"
    agent: "{agent-name}"
    phase: "{pipeline-step-label}"
    event: "{event-type}"
    message: "{human-readable description}"
    verbose_only: false
```

### Summary Block (updated by orchestrator at delivery)

```yaml
summary:
  status: "success|failure|in-progress"
  total_duration_s: {number}
  pipeline_flow: "{brief(OK) -> research(6 blocks) -> strategy(12 slides) -> review(pass) -> build(done)}"
```

## Event Types

| Event | When | Example |
|-------|------|---------|
| phase_start | Agent begins work | "Starting story review of 12-slide deck" |
| phase_end | Agent completes | "Build complete. Self-check: 5/5 passed." |
| phase_skip | Step skipped | "No brand.yaml found, skipping brand check" |
| decision | Agent makes a choice | "SCQA applied: C-Suite audience, clear complication" |
| validation | Check result | "Check 3 template-adherence: PASS" |
| error | Something failed | "check-contrast.js returned non-zero exit" |
| user_action | User intervened | "User approved deck-plan after round 2" |
| artifact_written | File created/modified | "presentation.html written -- 12 slides" |

## Per-Agent Event Taxonomy

| Agent | Events |
|-------|--------|
| presentation-researcher | phase_start, phase_end, artifact_written |
| brand-checker | phase_start, phase_end, artifact_written |
| brand-profiler | phase_start, phase_end, artifact_written |
| narrative-planner | phase_start, decision, phase_end, artifact_written |
| presentation-architect | phase_start, validation (per check), phase_end, artifact_written |
| presentation-critic | phase_start, validation (per check), phase_end, artifact_written |
| presentation-builder | phase_start, decision (deviations), artifact_written, phase_end |
| slide-stylist | phase_start, artifact_written, phase_end |
| slide-editor | phase_start, decision, artifact_written, phase_end |
| presentation-reviewer | phase_start, validation (per check), phase_end, artifact_written |

## Append Pattern

Agents append using Bash cat-append to avoid full file parse/write:

```bash
cat >> projects/{name}/.pipeline/build-log.yaml << 'ENTRY'
  - ts: "2026-04-06T14:30:00Z"
    agent: "presentation-builder"
    phase: "build"
    event: "phase_end"
    message: "Built 12 slides, all class names match catalog"
    verbose_only: false
ENTRY
```

CRITICAL: Use exactly 2-space indentation. Malformed YAML breaks the orchestrator summary reader.

## verbose_only Field

Both normal and verbose modes write identical build-log.yaml files. The `verbose_only` field controls what the orchestrator echoes to the user in conversation:
- `false` -- always shown to user (phase transitions, final outcomes)
- `true` -- only shown in verbose mode (per-slide progress, individual check results)

## Initialization Template

Orchestrator creates this file before spawning the first agent:

```yaml
meta:
  project: "{name}"
  started: "{ISO-8601}"
  mode: "normal"

entries: []

summary:
  status: "in-progress"
  total_duration_s: 0
  pipeline_flow: "started"
```

## Guard Pattern

Agents MUST handle the case where build-log.yaml does not exist (e.g., direct agent invocation outside the pipeline, or refine-deck flow):

```bash
[ -f projects/{name}/.pipeline/build-log.yaml ] || echo 'meta:
  project: "{name}"
  started: "unknown"
  mode: "normal"

entries: []

summary:
  status: "in-progress"
  total_duration_s: 0
  pipeline_flow: "direct-invocation"' > projects/{name}/.pipeline/build-log.yaml
```
```

The document must be comprehensive but concise -- agents reference this during execution. No prose filler.
  </action>
  <verify>
    <automated>test -f references/build-log-format.md && grep -c "phase_start\|phase_end\|artifact_written\|verbose_only\|decision\|validation\|error\|user_action" references/build-log-format.md</automated>
  </verify>
  <acceptance_criteria>
    - references/build-log-format.md exists and is >40 lines
    - Contains all 8 event types: phase_start, phase_end, phase_skip, decision, validation, error, user_action, artifact_written
    - Contains the per-agent event taxonomy table with all 10 agents listed
    - Contains the YAML schema with meta, entries, and summary blocks
    - Contains the Bash cat-append pattern example
    - Contains the guard pattern for missing build-log.yaml
    - Contains the verbose_only field explanation
    - Contains the initialization template
  </acceptance_criteria>
  <done>build-log-format.md is the canonical schema reference document that all agents and the orchestrator can reference for build-log.yaml structure, event types, and append patterns</done>
</task>

</tasks>

<verification>
- `node tools/check-contrast.js 2>&1 | head -5` runs without "themes" path errors
- `python3 tools/capture-slides.py --help` shows usage text
- `references/build-log-format.md` contains all 8 event types and 10-agent taxonomy
</verification>

<success_criteria>
- check-contrast.js references brands/ not themes/
- capture-slides.py is executable with --help and contains Playwright screenshot logic
- build-log-format.md documents the complete YAML schema referenced by D-05
</success_criteria>

<output>
After completion, create `.planning/phases/06-review-pipeline/06-01-SUMMARY.md`
</output>
