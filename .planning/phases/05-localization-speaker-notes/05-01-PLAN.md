---
phase: 05-localization-speaker-notes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/_skeleton.html
  - templates/title.html
  - templates/section-break.html
  - templates/image-full-bleed.html
  - templates/metrics.html
  - templates/agenda.html
  - templates/summary.html
  - templates/contact.html
  - templates/timeline.html
  - docs/german-typography.md
autonomous: true
requirements:
  - LANG-01
  - LANG-02

must_haves:
  truths:
    - "All 14 component templates have overflow-wrap: break-word and hyphens: auto on their text containers"
    - "The skeleton template contains a global German text handling CSS block that all new components inherit automatically"
    - "All template example content uses proper Unicode umlauts (no ae/ue/oe substitutions)"
    - "A German typography conventions reference document exists for AI assistants to consult"
  artifacts:
    - path: "templates/_skeleton.html"
      provides: "Global German text handling CSS block"
      contains: "German text handling"
    - path: "docs/german-typography.md"
      provides: "German typography conventions reference for AI assistants"
      contains: "Anführungszeichen"
  key_links:
    - from: "templates/_skeleton.html"
      to: "all 14 component templates"
      via: "CSS inheritance from .reveal .slides section"
      pattern: "\\.reveal .slides section"
---

<objective>
Add global German text handling CSS to the skeleton template, fix all templates missing hyphens/overflow-wrap, replace ASCII-substituted umlauts with proper Unicode characters, and create a German typography conventions reference document.

Purpose: Ensure all 14 components handle German text (130-300% expansion) without overflow or visual breakage, and provide AI assistants with a reference for correct German typographic conventions.
Output: Updated skeleton template with global German CSS, 7 fixed templates, German typography reference document.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-localization-speaker-notes/05-CONTEXT.md
@templates/_skeleton.html
@templates/title.html
@templates/metrics.html
@templates/agenda.html
@templates/contact.html
@templates/summary.html
@templates/section-break.html
@templates/image-full-bleed.html
@templates/timeline.html
@projects/example/presentation.html (lines 289-295 for global German CSS pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add global German text handling CSS to skeleton and fix all templates</name>
  <files>
    templates/_skeleton.html,
    templates/title.html,
    templates/section-break.html,
    templates/image-full-bleed.html,
    templates/metrics.html,
    templates/agenda.html,
    templates/summary.html,
    templates/contact.html,
    templates/timeline.html
  </files>
  <read_first>
    templates/_skeleton.html,
    templates/title.html,
    templates/section-break.html,
    templates/image-full-bleed.html,
    templates/metrics.html,
    templates/agenda.html,
    templates/summary.html,
    templates/contact.html,
    templates/timeline.html,
    templates/text-heavy.html,
    templates/two-column.html,
    templates/comparison.html,
    templates/framework.html,
    templates/quote.html,
    templates/card-grid.html
  </read_first>
  <action>
    **Part A: Add global German text handling CSS to skeleton template (per D-05)**

    In `templates/_skeleton.html`, add the following CSS block AFTER the "Base slide styling" section (after the `h1-h6` rule, around line 281) and BEFORE the closing `</style>` tag:

    ```css
    /* <!-- GERMAN TEXT HANDLING --> */

    /* Global German text handling — ensures compound words and long text
       break gracefully across all components. Requires lang="de" on <html>
       for hyphens: auto to work correctly. */
    .reveal .slides section {
      overflow-wrap: break-word;
      word-wrap: break-word;
      hyphens: auto;
      -webkit-hyphens: auto;
    }
    ```

    **Part B: Fix ASCII-substituted umlauts in template example content (per D-06)**

    Replace these exact strings in the listed files:

    `templates/metrics.html`:
    - Line 107: `Geschaeftsentwicklung Q3` -> `Geschäftsentwicklung Q3`
    - Line 119: `Verfuegbarkeit` -> `Verfügbarkeit`

    `templates/agenda.html`:
    - Line 137: `Naechste Schritte` -> `Nächste Schritte`

    `templates/contact.html`:
    - Line 33 (comment): `Geschaeftsfuehrerin` -> `Geschäftsführerin`
    - Line 102: `Geschaeftsfuehrerin Digitale Transformation` -> `Geschäftsführerin Digitale Transformation`

    `templates/summary.html`:
    - Line 35 (comment example): `Naechste Schritte` -> `Nächste Schritte`
    - Line 129: `Naechste Schritte` -> `Nächste Schritte`

    `templates/timeline.html`:
    - Line 156: `Einfuehrung` -> `Einführung`

    **Part C: Verify the 7 templates that already have overflow-wrap + hyphens**

    The following templates already have per-component `overflow-wrap: break-word` and `hyphens: auto` — verify they are present and do NOT remove them (they serve as fallback if the global rule is overridden):
    - text-heavy.html, two-column.html, comparison.html, framework.html, quote.html, card-grid.html, timeline.html

    The following 7 templates are MISSING per-component overflow-wrap/hyphens. Because the global rule (Part A) now covers them, no per-component addition is needed. But verify the global rule inherits properly (it does via `.reveal .slides section`).

    Templates that were missing: title.html, section-break.html, image-full-bleed.html, metrics.html, agenda.html, summary.html, contact.html

    Note: `title.html` already has `overflow-wrap: break-word` on `h1` but NOT `hyphens: auto`. The global rule covers this.
  </action>
  <verify>
    <automated>grep -c "German text handling" templates/_skeleton.html | grep -q "1" && grep -c "hyphens: auto" templates/_skeleton.html | grep -q "1" && echo "PASS: skeleton has German text block" || echo "FAIL"</automated>
    <automated>grep -c "Geschäftsentwicklung" templates/metrics.html | grep -q "1" && grep -c "Verfügbarkeit" templates/metrics.html | grep -q "1" && echo "PASS: metrics umlauts fixed" || echo "FAIL"</automated>
    <automated>grep -c "Nächste Schritte" templates/agenda.html | grep -q "1" && echo "PASS: agenda umlauts fixed" || echo "FAIL"</automated>
    <automated>grep -c "Geschäftsführerin" templates/contact.html && echo "PASS: contact umlauts fixed" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - templates/_skeleton.html contains the exact string `/* <!-- GERMAN TEXT HANDLING --> */`
    - templates/_skeleton.html contains `.reveal .slides section {` with `overflow-wrap: break-word` and `hyphens: auto` and `-webkit-hyphens: auto` inside that block
    - templates/metrics.html contains `Geschäftsentwicklung` (not `Geschaeftsentwicklung`)
    - templates/metrics.html contains `Verfügbarkeit` (not `Verfuegbarkeit`)
    - templates/agenda.html contains `Nächste Schritte` (not `Naechste Schritte`)
    - templates/contact.html contains `Geschäftsführerin` (not `Geschaeftsfuehrerin`) in both comment and HTML
    - templates/summary.html contains `Nächste Schritte` (not `Naechste Schritte`) in both comment example and HTML
    - templates/timeline.html contains `Einführung` (not `Einfuehrung`)
    - No template file contains the ASCII patterns `Geschaeftsfuehr`, `Naechste`, `Verfuegbar`, or `Einfuehr`
  </acceptance_criteria>
  <done>Skeleton template has global German text handling CSS. All 8 files with ASCII umlauts are fixed to use proper Unicode. The global CSS rule ensures all components inherit overflow-wrap and hyphens automatically.</done>
</task>

<task type="auto">
  <name>Task 2: Create German typography conventions reference document</name>
  <files>docs/german-typography.md</files>
  <read_first>
    templates/_skeleton.html,
    .planning/phases/05-localization-speaker-notes/05-CONTEXT.md
  </read_first>
  <action>
    Create `docs/german-typography.md` as the German typography conventions reference per D-01, D-02, D-03. This is framework documentation that any AI assistant (not just Claude) reads when generating German presentations.

    The document must contain these sections with these exact conventions:

    ```markdown
    # German Typography Conventions

    Reference for AI assistants generating German-language presentations.
    Apply these rules automatically when `lang="de"` is set on the HTML document.

    ## Quotation Marks (Anführungszeichen)

    German uses lower-opening and upper-closing quotation marks:
    - Primary: „Zitat" (U+201E opening, U+201C closing)
    - Nested: ‚Zitat' (U+201A opening, U+2018 closing)
    - NEVER use English-style "straight quotes" or "curly quotes"

    Example: „Die Digitalisierung ist kein Projekt, sondern eine ‚Reise'."

    ## Dashes (Gedankenstrich)

    - Em dash (Geviertstrich): Not used in German typography
    - En dash (Gedankenstrich, U+2013): Used with spaces for parenthetical clauses
      - Correct: „Unsere Strategie – basierend auf Marktanalysen – zeigt klare Vorteile."
    - Hyphen: Used for compound words: „IT-Infrastruktur", „End-to-End-Lösung"
    - Range dash (no spaces): „2024–2026", „S. 12–15"

    ## Numbers and Formatting

    ### Decimal Separator
    - German uses comma: 3,7% (not 3.7%)
    - On slides: „+23,5% Umsatzwachstum"

    ### Thousands Separator
    - Use thin space (U+202F) or regular space: 1 000 000 (not 1.000.000 or 1,000,000)
    - On metric slides: „4,2 Mio." or „1 250 000"

    ### Currency
    - Euro after number with space: 2,5 Mio. EUR or 2,5 Mio. €
    - Ranges: 1,2–1,5 Mio. EUR

    ### Percentages
    - Space before %: 23,5 % (formal) or 23,5% (slides — no space is acceptable for visual density)

    ## Abbreviations

    - Thin space between parts: z. B. (zum Beispiel), d. h. (das heißt), u. a. (unter anderem)
    - Common consulting abbreviations: Mio. (Millionen), Mrd. (Milliarden), ca. (circa), ggf. (gegebenenfalls)
    - NEVER use "z.B." without space — always "z. B."

    ## Eszett (ß) and Swiss German

    - Standard German (de-DE): Use ß after long vowels and diphthongs
      - Correct: „Maßnahme", „Straße", „gemäß"
    - Capitalization: SS (not SZ) — „STRASSE" not „STRAßE" (note: capital ß exists but is rarely used in presentations)
    - Swiss German (de-CH): Replace ß with ss — „Massnahme", „Strasse" (out of scope for v1)

    ## Date and Time

    - Long format: 25. März 2026 (day. month year)
    - Short format: 25.03.2026 (DD.MM.YYYY)
    - Quarter references: Q3 2026, 3. Quartal 2026
    - Time: 14:30 Uhr (24-hour format with Uhr suffix)

    ## Capitalization

    - All nouns capitalized: „Die Digitalisierung der Geschäftsprozesse"
    - Formal address: „Sie", „Ihr", „Ihnen" (always capitalized)
    - Slide titles: Sentence case, not Title Case — „Strategische Neuausrichtung" not „Strategische neuausrichtung"

    ## Compound Words

    - German creates long compounds: „Unternehmensdigitalisierungsstrategie"
    - For slides, prefer hyphenated breaks at natural boundaries for readability:
      „Unternehmens-Digitalisierungsstrategie"
    - CSS handles overflow via `overflow-wrap: break-word` and `hyphens: auto`

    ## Slide-Specific Conventions

    ### Action Titles (McKinsey-style)
    - Full sentences as slide titles: „Die Marktanalyse zeigt drei zentrale Handlungsfelder"
    - Not: „Marktanalyse" (too vague) or „Three Key Areas" (English pattern)

    ### Bullet Points
    - End with period if full sentences
    - No period if fragments/keywords
    - Consistent within a slide — do not mix

    ### Metric Labels
    - Use German abbreviations: „Mio.", „Mrd.", „Tsd."
    - Decimal comma: „+23,5%", „3,7x", „98,9%"
    - Space in large numbers on KPI slides: „4 200" or „4,2 Mio."
    ```

    Ensure the file is comprehensive, well-formatted markdown, and usable by any AI assistant — not Claude-specific.
  </action>
  <verify>
    <automated>test -f docs/german-typography.md && grep -c "Anführungszeichen" docs/german-typography.md | grep -q "1" && grep -c "Gedankenstrich" docs/german-typography.md | grep -q "1" && grep -c "3,7%" docs/german-typography.md | grep -qv "0" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File docs/german-typography.md exists
    - Contains section header `## Quotation Marks (Anführungszeichen)` with U+201E / U+201C examples
    - Contains section header `## Dashes (Gedankenstrich)` with en dash (U+2013) examples
    - Contains section `## Numbers and Formatting` with decimal comma (3,7%) and space thousands separator (1 000 000)
    - Contains section `## Abbreviations` with `z. B.` spacing rule
    - Contains section `## Eszett` with ß usage rules
    - Contains section `## Date and Time` with `25. März 2026` format
    - Contains section `## Compound Words` with hyphenation guidance
    - Contains section `## Slide-Specific Conventions` with action title and metric label guidance
    - File is at least 80 lines long
  </acceptance_criteria>
  <done>German typography conventions reference document exists at docs/german-typography.md, covering quotation marks, dashes, number formatting, abbreviations, eszett, dates, compound words, and slide-specific conventions. Any AI assistant can consult this when generating German presentations.</done>
</task>

</tasks>

<verification>
1. `grep -rn "Geschaeftsfuehr\|Naechste\|Verfuegbar\|Einfuehr" templates/*.html` returns no results (all ASCII umlauts fixed)
2. `grep -c "German text handling" templates/_skeleton.html` returns 1 (global block present)
3. `grep -c "hyphens: auto" templates/_skeleton.html` returns at least 1 (global rule present)
4. `test -f docs/german-typography.md` succeeds (reference document exists)
5. `wc -l docs/german-typography.md` returns 80+ lines (comprehensive document)
</verification>

<success_criteria>
- Skeleton template has global German text handling CSS block that all components inherit
- All 14 templates render with overflow-wrap and hyphens (7 via per-component rules, 7 via global inheritance)
- Zero ASCII-substituted umlauts remain in any template file
- German typography reference document covers all conventions listed in D-02
</success_criteria>

<output>
After completion, create `.planning/phases/05-localization-speaker-notes/05-01-SUMMARY.md`
</output>
